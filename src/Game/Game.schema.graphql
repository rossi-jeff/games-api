type Game {
  Id: Int!
  Name: String
  CreatedAt: Date
  UpdatedAt: Date
}

type Query {
  games: [Game]
  game(Id: Int!): Game
}

type Mutation {
  gameCreate(Name: String!): Game
  gameUpdate(Id: Int!, Name: String!): Game
}
