type Word {
  Id: Int!
  Word: String
  Length: Int
  CreatedAt: Date
  UpdatedAt: Date
}

type WordStat {
  Length: Int
  Count: Int
}

input WordFilter {
  Min: Int
  Max: Int
  Length: Int
}

type Query {
  wordRandom(filter: WordFilter): Word
  word(Id: Int!): Word
  wordStats: [WordStat]
}
